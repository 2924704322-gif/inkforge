# ─────────── Python ───────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# ─────────── Node / Electron ───────────
node_modules/
out/
dist/
apps/desktop/release/
# TS 增量编译缓存（提交过一次，应从源码树移除）
*.tsbuildinfo

# ─────────── 密钥（绝不入库） ───────────
.env
.env.*
!.env.example

# ─────────── 引擎运行时数据 ───────────
# 派生数据（向量库 / checkpoint / 日志，可随时重建）
engine/data/runtime/
*.sqlite
*.sqlite3
*.sqlite-*
# 蒸馏技能包（可由 distill 重建）
engine/data/skills/
# 用户创作数据（每本小说目录内另有独立 Git 仓库承担写作历史）
engine/data/novels/
engine/data/custom_skills/
engine/data/materials/
engine/data/learning/
engine/data/config/
# 工作区会话（墨师全域总控的对话记录，属用户数据）
engine/data/workspace/
# ★ 兜底：整个 engine/data/ 一律不入库（将来新增子目录也自动排除，防泄露用户作品或密钥）
engine/data/

# ─────────── 测试与临时产物 ───────────
e2e-shots/
smoke-output/
engine/smoke-reports/
engine/.smoke/
# 冒烟测试写出的临时文件（历史遗留：曾经把 e2e 日志写在仓库根内）
engine/e2e.txt
engine/smoke-state.json
engine/smoke-state.json
docs/_*.png
docs/step*.png

# ─────────── IDE / 系统 ───────────
.idea/
.vscode/
.DS_Store
Thumbs.db

# �ύ��Ϣ��ʱ�ļ�
COMMIT_MSG_TMP.txt
.git/COMMIT_MSG_TMP
# ─────────── 本地内部文档 ───────────
# docs/ 是本地内部文档（架构/状态/测试报告/历史分析），**不随公开仓库分发**。
# 注意：scripts/check_doc_paths.py 已适配——该目录不存在时自动跳过相关引用校验。
docs/