# Inkforge 引擎依赖锁文件（S1-1）
#
# 由开发验证环境（conda env langchain1.2 / Python 3.13.12）实测导出并人工收敛。
# 用途：`pip install -r engine/requirements.lock` 得到与验证环境一致的依赖树，
# 消除 requirements.txt 只写 `>=` 带来的「构建不可复现」问题。
#
# 更新方式：升级某个包后重新比对 `pip freeze`，并跑 scripts/verify_env.py + pytest。

# ── Web 服务 ──
fastapi==0.135.1
starlette==1.3.1
uvicorn==0.46.0
markdown==3.10.2

# ── Agent 编排 ──
langgraph==1.1.2
langgraph-checkpoint-sqlite==3.1.0

# ── 模型接入 ──
openai==2.26.0
anthropic==0.84.0

# ── 记忆层 ──
chromadb==1.5.9
numpy==2.5.1
rank-bm25==0.2.2
jieba==0.42.1

# ── MD 处理 ──
python-frontmatter==1.3.0
GitPython==3.1.57
PyYAML==6.0.3

# ── 配置 ──
pydantic==2.12.5
pydantic-settings==2.12.0
python-dotenv==1.2.1

# ── CLI ──
typer==0.24.1
rich==14.3.3

# ── 测试（CI 使用） ──
pytest==9.0.3
httpx==0.28.1

# ── 说明 ──
# ebooklib 未在验证环境安装（TXT 蒸馏可用，EPUB 蒸馏会明确报缺库）。
# 需要 EPUB 输入时执行：pip install ebooklib==0.18 并同步补入本文件。
